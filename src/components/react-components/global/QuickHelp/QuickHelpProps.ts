export type QuickHelpProps = {
  headerText: string;
  helpText: string;

  windowMinHeight?: string;
  windowMaxHeight?: string;
  windowMinWidth?: string;
  windowMaxWidth?: string;

  visible: boolean;
};
