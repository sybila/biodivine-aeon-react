import type { ModelEditorOtherStringsInt } from './ModelEditorOtherStrings/ModelEditorOtherStringsInt';

/** Class which provides uncategorized strings (not tooltips, help texts....) for different parts of the app.  */
export interface OtherStringsInt {
  /** Placeholder for model editor page. */
  ModelEditorOtherStrings: ModelEditorOtherStringsInt;
}
